version: 1
applications:
  - appRoot: kernel-claw-industrial-funnel/kernel-claw-industrial-funnel
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci || npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
